/**
 * Reminders Page
 *
 * TODO(Olyvia):
 * 1. Fetch reminders for official unfinished actions.
 * 2. Exclude Done actions and actions without deadlines.
 * 3. Display urgency based on workspace timezone.
 * 4. Support read and unread states.
 * 5. Navigate to the linked action item.
 */

export default function RemindersPage() {
  return (
    <section>
      <h1>Reminders</h1>
      <p>Reminder implementation is not available yet.</p>
    </section>
  );
}