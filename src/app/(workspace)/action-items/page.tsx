/**
 * Action Items Page
 *
 * TODO(Olyvia):
 * 1. Fetch official action items only.
 * 2. Group items into To Do, In Progress, Blocked, and Done.
 * 3. Add project and status filters.
 * 4. Keep Overdue as urgency, not workflow status.
 * 5. Support create, edit, delete, and status update operations.
 * 6. Preserve meeting source linkage.
 */

export default function ActionItemsPage() {
  return (
    <section>
      <h1>Action Items</h1>
      <p>Action item management is not available yet.</p>
    </section>
  );
}