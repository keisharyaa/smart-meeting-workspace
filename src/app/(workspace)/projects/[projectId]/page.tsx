/**
 * Project Detail Page
 *
 * TODO(Olyvia):
 * 1. Read projectId from route params.
 * 2. Fetch the project through feature queries.
 * 3. Show related published meetings and official actions.
 * 4. Support edit, archive, restore, reopen, and complete actions.
 * 5. Block completion while official unfinished actions remain.
 * 6. Show not-found and error states safely.
 */

export default function ProjectDetailPage() {
  return (
    <section>
      <h1>Project Detail</h1>
      <p>Project detail implementation is not available yet.</p>
    </section>
  );
}