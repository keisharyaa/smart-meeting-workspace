/**
 * TODO(Olyvia):
 * 1. Return reminders for official unfinished actions only.
 * 2. Exclude actions with no deadline.
 * 3. Exclude done actions.
 * 4. Include linked action navigation data.
 * 5. Support unread count.
 */
export async function getActiveReminders(): Promise<unknown[]> {
  return [];
}
